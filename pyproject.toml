[project]
name = "parser-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jmespath>=1.0.1",
    "psycopg2>=2.9.11",
    "python-decouple>=3.8",
]
